namespace gdjs {
  // Runtime implementation is intentionally centralized in
  // `navmeshruntimebehavior.ts` for deterministic load order.
  // This file exists to keep parity with PathfindingBehavior extension layout.
}
